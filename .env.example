# MoviCol — Variables de entorno
# Copiar a .env y completar valores

# PostgreSQL + PostGIS
POSTGRES_USER=movicol
POSTGRES_PASSWORD=movicol_dev
POSTGRES_DB=movicol

# OpenAI (opcional — sin esto el agente usa respuestas rule-based)
OPENAI_API_KEY=sk-xxx

# AI Service
MODEL_PATH=models/gat_best.pt
GRAPH_PATH=models/graph_clean.graphml
